function progressgrid() {
	/*
	 * Internal variables
	 */


	/*
	 * Main function object, which draws/updates the progress bar
	 */
	function my(container, something) {
		console.log(container, something);
	}
	return my;
}

